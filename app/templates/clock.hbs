<div class="actions">
  <button {{action "start"}} {{bind-attr class=":btn :btn-primary isStartButtonEnabled::disabled"}}>Start</button>
  <button {{action "stop"}} {{bind-attr class=":btn isStopButtonEnabled::disabled"}}>Stop</button>
</div>
