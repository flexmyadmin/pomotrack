import {
  timeFormat
} from 'pomotrack/helpers/time-format';

module('TimeFormatHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = timeFormat(42);
  ok(result);
});
